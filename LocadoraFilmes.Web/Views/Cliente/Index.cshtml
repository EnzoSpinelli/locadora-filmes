<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title></title>
</head>
<body>

</body>
</html>

@model IEnumerable<LocadoraFilmes.Web.Models.Cliente>

<h2>Clientes</h2>
<p>
    @Html.ActionLink("Adicionar Novo Cliente", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Nome)</th>
            <th>@Html.DisplayNameFor(model => model.CPF)</th>
            <th>Operações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Nome)</td>
                <td>@Html.DisplayFor(modelItem => item.CPF)</td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.ClienteId }, new { @class = "btn btn-success" }) |
                    @Html.ActionLink("Detalhes", "Details", new { id = item.ClienteId }, new { @class = "btn btn-info" }) |
                    @Html.ActionLink("Deletar", "Delete", new { id = item.ClienteId }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>